<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalprojectmabna">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".myProfileInfor"></activity>
        <activity android:name=".NewOfficeActivity" />
        <activity android:name=".officesActivity" />
        <activity android:name=".rentsDetails" />
        <activity android:name=".RentsPage" />
        <activity android:name=".Price" />
        <activity android:name=".calculate" />
        <activity android:name=".optionsActivity" />
        <activity android:name=".logInPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>